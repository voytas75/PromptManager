# PromptManager — standalone Notes CLI v1

Status: delivered — `ea1b0231885fb43a0260a0c3bb812efd0628ffc2` on `origin/master`; exact-SHA [Quality Gates 36235357930](https://github.com/voytas75/PromptManager/actions/runs/36235357930) successful. Native Windows acceptance on a SQLite backup completed separately.
Product authority: `docs/product-ssot.md` (assets first, automation subordinate). Starting revision: `527bbc36ab3492af68bd104e53d435ba859cb84c`.

## Confirmed baseline and corrected contract

- GUI Notes owns independent `PromptNote` records in the existing SQLite `prompt_notes` table (`id`, `note`, `created_at`, `last_modified`); no prompt relationship, title, or soft-delete. Reuse the table/model; do not create `notes` or change schema, GUI, prompts, Chroma, or SSOT.
- Public family: `prompt-manager note [--limit 20] [--json]`, `note add`, `note show <uuid>`, `note find <literal> [--limit 20]`, `note edit <uuid>`, `note delete <uuid> [--yes]`. Root, family, and leaf help must work through both supported entrypoints.
- `show/edit/delete` use full canonical UUIDs (no prefix lookup). List and find are newest first (`last_modified DESC`, ID tie-break), bounded to 1–100 entries. Literal `find` escapes SQL LIKE wildcard characters; SQLite's default case-insensitivity is ASCII-only; do not claim Unicode casefolding.
- Add and edit accept exactly one of positional text, `--body`, UTF-8 `--file`, `--from-stdin`. Whitespace-only input fails before mutation, matching GUI validation. Text is stripped like GUI. Unreadable/non-UTF-8/oversized files fail cleanly. `--json` is an output selector, not an input source.
- Text output is default. JSON results have one document on stdout and no stderr on success; errors return nonzero and one bounded JSON error on stderr in JSON mode. List/find expose compact previews and IDs; show exposes full text and timestamps. Add/edit return ID and timestamps; delete returns ID. No content in error diagnostics.
- Delete is hard delete via the existing table, matching GUI; require explicit `--yes` for non-interactive execution and for JSON mode, otherwise ask on TTY with a default-No answer. Cancellation must leave record intact.
- The `note` family must dispatch before the full manager/factory imports in both `python -m main` and installed `prompt-manager`. Reads must not initialize Chroma/provider services, schema, or a missing SQLite database. Writes require an existing selected SQLite database with `prompt_notes`; no implicit new catalog creation. Smoke only against disposable isolated data.

## Checkpoints

1. Audit schema/GUI/entrypoints — confirmed from model, repository, GUI and parser. Repository CRUD tests: 4 passed in targeted selection.
2. Read-only family: real-process RED on missing parser route, then list/show/find and root/family/leaf help through both entrypoints. Existing and missing database probes pass; no Chroma directory. Status: implemented, focused green.
3. Mutations: RED pack covers add/edit/delete, input conflicts and non-TTY delete guard; isolated SQLite lifecycle and repository interoperability pass. Independent review found three error-boundary defects (configuration warning on JSON success, malformed stored rows causing traceback, text argparse echo of extra note body). All three reproduced RED and fixed GREEN, plus a parser non-note isolation regression. Corrected the original contract's positional edit body with a separate RED → GREEN regression. Status: verified locally.
4. Final local gates (after review corrections): installed and module CLI help smoke, 18 real-process note tests, Ruff check/format, configured Pyright (`0 errors`), touched-file Pyright (`0 errors`), `uv lock --check`, full pytest (`1018 passed, 1 skipped`), core coverage (`81.66%`, threshold 80), `git diff --check`. Developer command index and Unreleased changelog synchronized. Local verification green; remote delivery and CI are recorded by their own exact revision.

## Delivery and native Windows acceptance

- The bounded code/docs slice reached `origin/master` as `ea1b0231885fb43a0260a0c3bb812efd0628ffc2`. Exact-SHA Quality Gates [36235357930](https://github.com/voytas75/PromptManager/actions/runs/36235357930) completed successfully; this is delivery evidence, separate from the local gates above.
- Windows checkout `D:\repos\PromptManager` was fast-forwarded to the same SHA. Read-only native CLI checks on the selected catalog found 35 notes and exercised list/show/find without mutating the selected catalog. A later read-only check again returned 35 notes. No note text or catalog path is recorded here.
- Native Windows functional smoke on a SQLite-backup copy of that catalog exercised bounded list/limit, show, literal find, add/edit through four input sources, input/error guards, non-TTY delete refusal and `delete --yes`. Synthetic changes were removed from the copy; all note rows on the copy matched its baseline, and a separate read-only comparison found the selected catalog's note rows unchanged and its prompt count unchanged. This did not compare every prompt field or other table. Both scratch test directories and the one-off script were subsequently removed.
- Scope of evidence: Windows `.venv` lacked `pytest`, so this was real-process CLI smoke, **not** a Windows pytest run. Interactive TTY deletion confirmation and a GUI acceptance run were not tested. A new note need not appear first in a list when existing records have later `last_modified` values; the list contract remains timestamp-descending rather than insertion order. Do not treat this observation alone as a defect.

The code and docs form one bounded slice. Future work (promote, tags, embeddings, import/export, notes doctor, GUI changes) is out of scope; no successor was chosen by this acceptance.
