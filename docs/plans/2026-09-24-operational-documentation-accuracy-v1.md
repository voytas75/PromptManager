# PromptManager — Operational documentation accuracy v1

Status: completed locally; delivery authorized (exact-SHA CI to verify after push)
Owner: Wojtek / Prompt Manager Team
Started: 2026-09-24
Canonical product direction: `docs/product-ssot.md`
Canonical near-term priorities: `docs/plans/2026-05-10-product-direction-ssot-next-cycle.md`

## Decision and boundary

Correct misleading instructions found in the read-only operational-documentation audit at `67863f8`. This is a documentation-only correction. Keep historical verification results, implementation behavior, release version, dependencies, GitHub workflow, security controls, and provider routing unchanged. No live provider calls or user data operations in this slice; commit and push require separate authorization, given after local completion.

Before editing a claim, compare it with the actual entrypoint/config/maintenance implementation. If behavior is ambiguous, narrow the claim rather than invent a new operational contract. The previous audit's tentative `.env.example` startup blocker was disproved in an isolated settings probe: `.env.example` alone loads without `config/config.json`; an explicitly exported missing config path fails closed.

## Work and acceptance

1. **Operator safety and configuration accuracy** — `docs/README-DEV.md`, `README-DEV.md`, and only if needed `README.md`: make JSON-over-env precedence and explicit config path behavior correct; describe `--print-settings` as a settings summary, not a connectivity or writability probe. Warn that `reembed` clears Chroma's collection before rebuilding, can partially fail, and needs a backup and an explicit decision (with provider cost possible). Correct any confirmed nonexistent telemetry toggle, missing optional embedding dependency, and unjustified embedding fallback promise. Preserve existing command names.
2. **Navigation and quality-gate accuracy** — make the roles of root `README-DEV.md` (short onboarding) and `docs/README-DEV.md` (technical reference) clear; distinguish the actual CI subset from full local Pyright, and clarify optional Nox installation and lockfile/CI parity. Remove the obsolete token-plan instruction as an active priority without erasing the historical proposal.
3. **Status/currentness closeout** — mark delivered 2026-09-22 ledgers completed; mark 2026-09-23 release actions in `docs/STATUS.md` as historical, not pending; clarify that the active near-term plan is not a historical authority. Keep the product rating SSOT and historical evidence unchanged.

After **each** item above: append a short outcome and actual verification to the ledger below, then move to the next. Verify exact diff scope, `git diff --check`, local document links, and absence of misleading active claims. This documentation-only task does not require paid or full runtime quality gates; use a provider-free focused check for relevant CLI/config claims if needed. Mark completed only after final read-only review and clean document checks. Do not commit or push without separate approval.

## Progress ledger

- 2026-09-24 — Planned. Repository `master` and `origin/master` both `67863f8f5e170266b4f411ea637174910072d749`; worktree initially clean. Isolated scratch probe of `.env.example` without `config/config.json` loaded settings; explicit missing `PROMPT_MANAGER_CONFIG_JSON` failed with `SettingsError`. No source or runtime changes.
- 2026-09-24 — Item 1 completed locally: corrected JSON-over-env precedence, bounded `--print-settings` semantics, explicit `reembed` reset/partial-failure/backup/provider-cost warning, missing sentence-transformers dependency, unsupported telemetry flag, and conditional LiteLLM fallback in `docs/README-DEV.md` plus short onboarding clarification in root `README-DEV.md`. Verified against `config/settings.py`, `main.py`, `cli/commands.py`, `core/prompt_manager/maintenance.py`, `core/embedding.py`, and `core/prompt_manager/backends.py`. Focused static assertions for each warning and `git diff --check` passed. `README.md` unchanged: its `.env.example` and provider-free-mode guidance did not make the disproved startup claim. No application, provider, or user-data operations.
- 2026-09-24 — Item 2 completed locally: root `README-DEV.md` now names the detailed reference and current product/plan owners; `docs/README-DEV.md` names its short entry point and replaces 0.23.1 as an active gate with historical context. GitHub workflow type scope is differentiated from full local Pyright; branch protection remains to verify separately. Nox is explicitly optional/not in `dev`; uv lockfile sync is no longer called exact pip-CI install parity. The old token-rollup document is a historical proposal, not a live roadmap. Focused assertions against `.github/workflows/quality-gates.yml`, `pyproject.toml`, and both guides plus `git diff --check` passed. No install or toolchain change.
- 2026-09-24 — Item 3 completed locally: both delivered 2026-09-22 ledgers now say completed; `docs/STATUS.md` keeps their results and the 2026-09-23 release evidence, but labels the prior 0.23.1 actions and local SHAs historical. The 2026-05-10 plan is still explicitly active. Verified with assertions on ledger headers, historical status labels and package-version cross-check (`0.23.3`), plus `git diff --check`. No change to `docs/product-ssot.md` or behavior.
- 2026-09-24 — Independent read-only review found two blockers in the candidate: JSON template `embedding_backend: litellm` overrides `.env.example`'s deterministic value, and the GUI snapshot archives only the SQLite main file (not WAL) while the repository uses WAL. Corrected both guides: explain the actual effective backend with an isolated scratch probe (env alone `deterministic`; env + copied JSON `litellm`; JSON explicitly changed to `deterministic` restores provider-free backend selection). `docs/README-DEV.md` now requires a consistent, verified SQLite+Chroma backup and warns that a running GUI snapshot alone may omit committed WAL data. Confirmed snapshot implementation in `core/prompt_manager/maintenance.py:522-553,644-651`, WAL mode in `core/repository/base.py:55-70`, and `git diff --check`. No backup or `reembed` command was executed.
- 2026-09-24 — Final local verification: provider-free targeted `QT_QPA_PLATFORM=offscreen .venv/bin/pytest -q tests/test_settings.py tests/test_main_entry.py tests/test_embedding_provider.py tests/test_cli_help_contract_docs.py` returned `180 passed` (exit 0). All local Markdown links in the six changed/new documentation files resolve; focused assertions checked backup/WAL caveat, effective backend precedence, `--print-settings`, `reembed`, CI install and historical status; `git diff --check` passed. Exact scope is five modified Markdown files plus this new ledger, with no application/config/CI changes.

## Completion / next decision

Items 1–3 completed and verified locally; independent review blockers corrected and checked. A separate commit/push decision was granted after local completion; verify remote SHA and exact-SHA CI after delivery. No live provider, backup, reset, or reembed was performed. Branch-protection/required-check settings remain to verify if someone needs to claim checks block merges; online-safe GUI snapshots require a separate code decision. No further product/runtime work is authorized by this ledger.
